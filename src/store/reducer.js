const defaultState = {
    inputValue: 'write',
    list: [
        '早上',
        '中午',
        '晚上',
    ],
};
const reducer = (state = defaultState, action) => {
    return state;
};
export default reducer;